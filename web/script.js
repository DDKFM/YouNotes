$(document).ready(function () {
    //entspricht dem window.onLoad = function(....
    /*
    Hier kommen zwei Codebeispiele zum Ajax-Aufruf mit jQuery

    $.ajax({
        type: "POST",
        url:"Hier URL für die Request angeben",
        data:"Hier kommen Daten rein z.B. POST-Data username=abcd&password=abcd",
        success: function (result) {
            //Dieser Code wird ausgeführt wenn die Antwort vom Server kommt
        }
    });
    //Alternativ:
    $.post(
        "URL",
        {//Post-Parameter
            name: "John",
            password : "abcd"
        },
        function(result) {
            //Dieser Code wird ausgeführt wenn die Antwort vom Server kommt
        });

     */
})